<% @title='New Charge Cycles' %>
<% if @charge_cycle.period_type == 'monthly' %>
  <%= render 'form_monthly', charge_cycle: @charge_cycle %>
<% else %>
  <%= render 'form', charge_cycle: @charge_cycle %>
<% end %>
