<div class='insert'>
  <br>
  <%= @template.notices.find(count).instruction %>
  <br>
</div>
<div class='widemargin'>
  <%= @template.notices.find(count).fill_in %> <%= @template.notices.find(count).sample %>
</div>
