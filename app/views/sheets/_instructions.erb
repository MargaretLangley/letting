<div class='insert'>
  <br>
  <%= Sheet.find(2).notices.find(count).instruction %>
  <br>
</div>
<div class='widemargin'>
  <%= Sheet.find(2).notices.find(count).fill_in %> <%= Sheet.find(2).notices.find(count).sample %>
</div>
