<h2>Charge Properties</h2>

<%= render 'properties/search'%>
<p>or</p>
<h3>Select Block</h3>

<% @blocks.each do |block| %>
  <%= link_to block.name, new_debt_generator_path(block,block: block.name ) %>
<% end %>