<%= render 'bubbles/help_inv',
  heading: 'Invoicing-1st Run ',
  lines: [ 'This form creates the first run of invoices. The first run are those which are not automatically paid into the bank.',
          'Fill in the property range. The search dates are used to find those accounts which are due. The invoice date is that used by the invoice. Two lines of comment are allowed.']
%>
