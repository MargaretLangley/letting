<%= render 'bubbles/help_intro',
  heading: 'Create New Client and Client Update',
  search_on: 'clients',
  lines: ['This form allows the creation of a new client or the update of an existing client. Client details are client ID, name and address.',
         'The search box at the top of the page is for client searches. Search by client name or ID.'],
  ikon:  [ IconData.new('fa fa-file-o', 'Link to view, if not greyed out'),
          IconData.new('fa fa-plus-square-o', 'Allows this otherwise blank line to be filled in'),
          IconData.new('fa fa-trash-o', 'Deletes item, if not greyed out')]
%>
